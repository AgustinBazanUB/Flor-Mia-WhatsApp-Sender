name: release contact export 0.9.5.3

on:
  push:
    branches:
      - feat/contact-export

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feat/contact-export
      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm
      - name: Apply 0.9.5.3 patch
        run: python scripts/release-0.9.5.3.py
      - name: Install locked dependencies
        run: npm ci
      - name: Verify before commit
        run: npm run verify
      - name: Commit release
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src tests manifest.json package.json package-lock.json scripts/validate-build.mjs README.md docs/whatsapp-contact-export.md docs/contact-export-release-notes-0.9.5.3.md
          git commit -m "fix: resolve labeled contacts from local WhatsApp store in 0.9.5.3"
          git push origin HEAD:feat/contact-export
