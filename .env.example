# Lista separada por comas de match patterns de Chrome.
# El build ya incluye producción y localhost desde config/allowed-origins.json.
# Utilizar solamente para agregar un Deploy Preview o un origen explícito.
FLORMIA_EXTRA_WEB_APP_ORIGINS=
