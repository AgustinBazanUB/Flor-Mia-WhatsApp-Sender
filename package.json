{
  "name": "flor-mia-whatsapp-sender",
  "version": "0.9.4.2",
  "private": true,
  "type": "module",
  "description": "Extensión privada Manifest V3 para ejecutar envíos preparados por Flor Mía.",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate:build": "node scripts/validate-build.mjs",
    "verify": "npm run typecheck && npm run lint && npm test && npm run build && npm run validate:build"
  },
  "devDependencies": {
    "@eslint/js": "^9.33.0",
    "@types/chrome": "^0.1.1",
    "@types/node": "^24.3.0",
    "esbuild": "^0.25.9",
    "eslint": "^9.33.0",
    "fake-indexeddb": "^6.2.2",
    "globals": "^16.3.0",
    "jsdom": "^26.1.0",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.40.0",
    "vitest": "^3.2.4"
  }
}
